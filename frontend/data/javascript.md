## Data types:

### Number:
```js
let n = 100;
n = 1.25;
```
We can make operations with numers:
```js
let n = 100 + 200;
//n = 300
```

BigInt, String, Boolean, Null, symbol, object **typeof operator**

var, let, const control flow loop function

##### async programming

callback promise async await — combinator semantics (`all` / `allSettled` / `any` / `race`) live in MDN, `developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise`

setTimeout setInterval

#### meta programming

proxy, reflect
