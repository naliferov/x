# glossary

Только то, чего нет в первом результате поиска. SOLID / GRASP / CAP / ACID / GoF - гуглятся быстрее, чем открывается эта заметка, поэтому их тут нет.

**concurrency vs parallelism** - один ресурс, много потребителей · несколько ресурсов, много потребителей.

**CI / CD / CD** - на каждый коммит собрать и протестить в общем репо · автодоставка на тест и пре-прод · автодоставка на прод.

## Процессор - карманы и шкаф

У процессора есть только регистры. Регистры держат всё, с чем он работает прямо сейчас: сложить два числа - оба грузятся из памяти в регистры, складываются, результат уезжает обратно в память или на стек.

Думай о себе как о процессоре. **Карманы - регистры, шкаф в комнате - память.** Подходишь к шкафу, достаёшь, кладёшь в карманы, идёшь работать с тем, что взял, потом кладёшь обратно, чтобы освободить карманы под новое. Карманов сильно меньше, чем ящиков. И к карманам не надо идти - они всегда на тебе. Поэтому это самая быстрая память.

LRU-кеши есть, но прямого доступа к ним нет - ими управляет шина памяти.

## Почему стек быстрее кучи

- непрерывный, фиксированного размера, а не куски страниц
- выделяется на старте программы и живёт до конца процесса - нечего отслеживать, чистить, грузить и выгружать, этим занимается менеджер процессов ОС
- максимальный размер задаётся на этапе компиляции; на некоторых архитектурах его вообще нельзя ресайзить динамически
- стек процесса обычно отгорожен guard-страницей (на архитектурах с MMU), чтобы ловить переполнение

**heap** - управляется GC либо руками, в зависимости от рантайма. Стек процессов ОС и стеки JS-виртуалок имеют мало общего.

## Интерпретация против компиляции

Parsing → AST → (Bytecode | JIT | Threaded code). Threaded code - способ сделать промежуточную ВМ рядом с байткодом.

## Правило доставки в op-based CRDT

Единственная часть темы, которую не вынуть из вики чтением по диагонали. `L` - локальный вектор времени (`L[i]` = сколько операций от узла i уже применено здесь), `v` - вектор, пришедший с операцией от отправителя k.

| условие | смысл |
| --- | --- |
| `L[k] == v[k] - 1` | все предыдущие операции отправителя уже применены: при `v[k]=2` нужно `L[k]=1` |
| `L[i] >= v[i]` для всех i ≠ k | всё, что отправитель считал сделанным, здесь уже есть |

Реплика инкрементит **свой** элемент вектора только когда сама порождает операцию. Вектор на трёх узлах `[2, 5, 3]` = узел 1 сделал 2 события, узел 2 - пять, узел 3 - три.

**Что требовать от эффекторов, в зависимости от канала:**

| доставка | требование |
| --- | --- |
| упорядоченная | конкурентные эффекторы коммутативны |
| неупорядоченная | все эффекторы коммутативны |
| возможны повторы | эффекторы идемпотентны |

И главное, что путают: CRDT дают **strong eventual consistency**, а не строгую. Нужна строгая при конкурентном доступе - это транзакции с уровнями изоляции, блокировки или консенсус (Raft / Paxos), не CRDT.

