**ES2026** Temporal API, explicit resource management (`using` / `await using`, Symbol.dispose), Error.isError()

**ES2025** iterator helpers (map / filter / take / drop / flatMap / reduce…), Set methods (union / intersection / difference / symmetricDifference / isSubsetOf…), Promise.try, RegExp.escape, JSON modules + import attributes (`import … with { type: 'json' }`), Float16Array, duplicate named capture groups, inline RegExp flag modifiers

**ES2024** Object.groupBy / Map.groupBy, Promise.withResolvers, resizable + transferable ArrayBuffer, Atomics.waitAsync, String.isWellFormed / toWellFormed, RegExp `v` flag (unicodeSets)

**ES2023** Array findLast / findLastIndex, change-array-by-copy (toSorted / toReversed / toSpliced / with), hashbang (`#!`) grammar, symbols as WeakMap keys

**ES2022** top-level await, class fields (public + private `#`), private methods, static init blocks, ergonomic brand check (`#x in obj`), Array.at / String.at, Object.hasOwn, RegExp `d` flag (match indices), Error cause

**ES2021** logical assignment (`&&=` `||=` `??=`), numeric separators (`1_000_000`), Promise.any + AggregateError, String.replaceAll, WeakRef + FinalizationRegistry

**ES2020** BigInt, optional chaining `?.`, nullish coalescing `??`, Promise.allSettled, globalThis, dynamic `import()`, String.matchAll, import.meta

**ES2019** Array.flat / flatMap, Object.fromEntries, String.trimStart / trimEnd, optional catch binding, stable Array.sort, Symbol.description

**ES2018** async iteration (`for await … of`), object rest / spread, Promise.finally, RegExp: named capture groups + lookbehind + dotAll (`s`) flag + unicode property escapes

**ES2017** async / await, Object.entries / values, Object.getOwnPropertyDescriptors, String padStart / padEnd, SharedArrayBuffer + Atomics, trailing commas in params

**ES2016** Array.prototype.includes, exponentiation operator (`**`)

**ES2015 (ES6)** the big one - let / const, arrow functions, classes, template literals, destructuring, default / rest / spread params, Promise, generators, iterators + `for … of`, Map / Set / WeakMap / WeakSet, Symbol, modules (import / export), Proxy / Reflect, block scoping, typed arrays

**ES4** abandoned (2008) - too ambitious; ideas later split into ES5 + ES6

**ES5.1 (2011)** editorial - ISO/IEC alignment

**ES5 (2009)** strict mode, JSON parse / stringify, Object.defineProperty + getters / setters, Array iteration (forEach / map / filter / reduce / indexOf), Function.prototype.bind, property descriptors

**ES3 (1999)** regular expressions, try / catch / finally, do-while, switch, richer String methods

**ES1 (1997)** first edition - Brendan Eich / Netscape, standardized by ECMA